name: Main CI and Releases

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Check
        run: cargo check --workspace

  test:
    name: Test Suite
    strategy:
      matrix:
        os: [ubuntu-latest, ubuntu-22.04-arm, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Run tests
        run: cargo test --workspace

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - name: Clippy check
        run: cargo clippy --all-targets --all-features -- -D warnings

  build:
    name: Build
    if: github.event_name == 'push'
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            mesio_artifact_name: mesio
            mesio_asset_name: mesio-linux-amd64
            strev_artifact_name: strev
            strev_asset_name: strev-linux-amd64
          - os: ubuntu-22.04-arm
            mesio_artifact_name: mesio
            mesio_asset_name: mesio-linux-arm64
            strev_artifact_name: strev
            strev_asset_name: strev-linux-arm64
          - os: windows-latest
            mesio_artifact_name: mesio.exe
            mesio_asset_name: mesio-windows-amd64.exe
            strev_artifact_name: strev.exe
            strev_asset_name: strev-windows-amd64.exe
          - os: macos-latest
            mesio_artifact_name: mesio
            mesio_asset_name: mesio-macos-amd64
            strev_artifact_name: strev
            strev_asset_name: strev-macos-amd64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - name: Build mesio release
        run: cargo build -p mesio --release

      - name: Build strev release
        run: cargo build -p strev --release

      - name: Upload mesio binary
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.mesio_asset_name }}
          path: target/release/${{ matrix.mesio_artifact_name }}
          if-no-files-found: error

      - name: Upload strev binary
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.strev_asset_name }}
          path: target/release/${{ matrix.strev_artifact_name }}
          if-no-files-found: error

  release:
    name: Release
    needs: [check, test, clippy, build]
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
      pull-requests: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List artifacts
        run: ls -R artifacts

      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          files: |
            artifacts/mesio-linux-amd64/mesio
            artifacts/mesio-linux-arm64/mesio
            artifacts/mesio-windows-amd64.exe/mesio.exe
            artifacts/mesio-macos-amd64/mesio
            artifacts/strev-linux-amd64/strev
            artifacts/strev-linux-arm64/strev
            artifacts/strev-windows-amd64.exe/strev.exe
            artifacts/strev-macos-amd64/strev
          draft: false
          prerelease: false